# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Hyprlock config for => 2k monitor resolutions                                                         
# Original config submitted by https://github.com/SherLock707

# Sourcing colors generated by wallust
source = $HOME/.config/hypr/wallust/wallust-hyprland.conf
$Scripts = $HOME/.config/hypr/scripts

general {
    grace = 1
    fractional_scaling = 2
    immediate_render = true
}

background {
    monitor =
    # NOTE: use only 1 path
	path = screenshot   # screenshot of your desktop
	#path = $HOME/.config/hypr/wallpaper_effects/.wallpaper_modified  # by wallpaper effects
        #path = $HOME/.config/hypr/wallpaper_effects/.wallpaper_current
    
    color = rgb(0,0,0) # color will be rendered initially until path is available

    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    blur_size = 5
    blur_passes = 3 # 0 disables blurring
    noise = 0.0117
    contrast = 1.3000 # Vibrant!!!
    brightness = 0.8000
    vibrancy = 0.2100
    vibrancy_darkness = 0.0
}


# Date
label {
    monitor =
    text = cmd[update:18000000] echo "<b> "$(date +'%A, %B %-d ')" </b>"
    color = #fff
    font_size = 24
    font_family = SF Pro Bold
    position = 0, -200
    halign = center
    valign = top
}

# Hour-Time
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%H" ) : "
#    text = cmd[update:1000] echo "$(date +"%I")" #AM/PM
	#color = rgba(255, 185, 0, .8)
    color = #fff
    font_size = 90 
    font_family = SF Pro Display ExtraBold 
    position = -70, 150
    halign = center
    valign = center
}

# Minute-Time
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%M")"
    #color = rgba(15, 10, 222, .8)
    color = #fff
    font_size = 90
    font_family = SF Pro Display ExtraBold 
    position = 120, 150
    halign = center
    valign = center
}

# Seconds-Time
#label {
#   monitor =
#   text = cmd[update:1000] echo "$(date +"%S")"
#    text = cmd[update:1000] echo "$(date +"%S %p")" #AM/PM
#   color = $color11
#    font_size = 90
#    font_family = JetBrainsMono Nerd Font ExtraBold
#    position = 200, -50
#    halign = center
#    valign = top
#}

# Put a picture of choice here. Default is the current wallpaper


#----------------------ORIGINAL------------------------------------
#image {
#    monitor =
#    path = $HOME/.config/hypr/wallpaper_effects/.wallpaper_current
#    size = 120
#    rounding = -1
#    border_size = 0
#    border_color = $color11
#    rotate = 0
#    reload_time = -1
#    position = 0, 320
#    halign = center
#    valign = bottom
#}
#------------------------------------------------------------------

image {
    monitor =
    path = /home/ffsalomon/Pictures/user_avatars/coffee.png
    size = 100
    border_size = 2
    border_color = $foreground
    position = 0, 0
    halign = center
    valign = center
}

# Desktop Environment
#image {
#    monitor =
#    path = /home/ffsalomon/Pictures/user_avatars/coffee.png
#    size = 75
#    border_size = 2
#    border_color = $foreground
#    position = -50, 50
#    halign = right
#    valign = bottom
#}

# USER

#-----------------------ORIGINAL-------------------------------

#label {
#    monitor=
#    text = 
#    color = $color13
#    font_size = 48
#    font_family = Victor Mono Bold Oblique
#    position = 0, 200
#    halign = center
#    valign = bottom

#label {
#    monitor =
#    text = $USER
#    color = $color13
#    color = $color13
#    font_size = 24
#    font_family = Victor Mono Bold Oblique
#    position = 0, 250
#    halign = center
#    valign = bottom
#}
#--------------------------------------------------------------

label {
    monitor =
    text = $USER
    color = #fff
    font_size = 22
    font_family = SF Display
    position = 0, -120
    halign = center
    valign = center
}

# INPUT FIELD
# ----------------------ORIGINAL-----------------------------
#input-field {
#    monitor =
#    size = 300, 60
#    outline_thickness = 1
#    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
#    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
#    dots_center = true
#    outer_color = $color11
#    inner_color = rgba(255, 255, 255, 0.1)
#    font_color = $color13
#    capslock_color = rgb(255,255,255)
#    fade_on_empty = false
#    font_family = Victor Mono Bold Oblique
#    placeholder_text = <i><span foreground="##ffffff99">Password</span></i>
#    hide_input = false
#    position = 0, 150
#    halign = center
#    valign = bottom
#}
#___________________________________________________________

# INPUT FIELD
input-field {
    monitor =
    size = 200, 50
    outline_thickness = 1
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.35 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(0 , 0, 0 , 0)
    inner_color = rgba(176, 172, 167, 0.5)
    font_color = rgb(255, 255, 255)
    fade_on_empty = false
    rounding = -1
    check_color = rgb(204, 136, 34)
    placeholder_text = <i><span foreground="##ffffff">Enter pass</span></i>
    hide_input = false
    position = 0, -180
    halign = center
    valign = center
}

# uptime
#label {
#    monitor =
#    text = cmd[update:60000] echo "<b> "$(uptime -p || $Scripts/UptimeNixOS.sh)" </b>"
#    color = $color13
#    font_size = 18
#    font_family = Victor Mono Bold Oblique
#    position = 0, 0
#    halign = right
#    valign = bottom
#}

# weather edit the scripts for locations
# weather scripts are located in ~/.config/hypr/UserScripts Weather.sh and/or Weather.py
# see https://github.com/JaKooLit/Hyprland-Dots/wiki/TIPS#%EF%B8%8F-weather-app-related-for-waybar-and-hyprlock
#label {
#    monitor =
#    text = cmd[update:3600] [ -f "$HOME/.cache/.weather_cache" ] && cat  "$HOME/.cache/.weather_cache"
#    color = #ffffff
#    font_size = 16
#    font_family = SF Display
#    text_align = right
#    position = -50, 0
#    halign = right
#    valign = bottom
#}
